{
  "name": "bl-bridge-broadcaster",
  "version": "1.0.0",
  "description": "This project sends messages from the bridge to the local kafka bus",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "João Esperacinha",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.2"
  },
  "dependencies": {
    "express": "^4.17.1",
    "fs": "0.0.1-security",
    "kafka-node": "^5.0.0",
    "node-cron": "^2.0.3"
  }
}
