export class Sample {
    constructor(deviceId, deviceSerialNumber, deviceType, reading, unit) {
        this.deviceId = deviceId;
        this.deviceSerialNumber = deviceSerialNumber;
        this.deviceType = deviceType;
        this.reading = reading;
        this.unit = unit;
    }
}